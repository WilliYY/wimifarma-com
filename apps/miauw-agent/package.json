{
  "name": "wimifarma-miauw-agent",
  "version": "0.16.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "check:persona": "node scripts/check-persona.mjs",
    "test": "npm run check && npm run check:persona",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@openai/agents": "0.11.4",
    "express": "^4.22.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3"
  }
}
